import { Typography } from '@material-ui/core';
import * as React from 'react';

export const Description: React.FunctionComponent = () => (
  <Typography variant="body1">Requires lines to be under a certain max length.</Typography>
);
